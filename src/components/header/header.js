import React from 'react';
import { SHeader } from '../styles';

const Header = () => {
    return (
        <SHeader>
            <h1>THE Rain Checker</h1>
        </SHeader>
    );
};

export default Header;